// token.helpers.ts — REMOVED in Phase 9 (OAuth Foundation)
//
// The email-based verification token flow (Level 2 auth) has been replaced by
// Google OAuth in Phase 9. The VerificationToken table has been dropped from the
// database schema, so the createVerificationToken function that lived here is no
// longer needed. This file is kept as a placeholder to record the history.
//
// If email-based verification is needed again in a future phase, re-add the
// VerificationToken model to schema.prisma and re-implement this helper.
