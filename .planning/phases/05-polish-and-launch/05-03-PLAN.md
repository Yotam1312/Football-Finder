---
phase: 05-polish-and-launch
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/TransportPage.tsx
  - frontend/src/App.tsx
  - frontend/src/components/Navbar.tsx
autonomous: true
requirements:
  - PAGE-01

must_haves:
  truths:
    - "Navigating to /transport shows the Transportation Guide page"
    - "Transport page has 3 section headers: Public Transit, Ride Services, Long-Distance Travel"
    - "Each service shows app icon area, app name, description, and link"
    - "Transport link is visible in the Navbar on every page"
  artifacts:
    - path: frontend/src/pages/TransportPage.tsx
      provides: "Transportation guide page with 3 sections and service cards"
    - path: frontend/src/App.tsx
      provides: "/transport route"
    - path: frontend/src/components/Navbar.tsx
      provides: "Transport link in nav"
  key_links:
    - from: frontend/src/components/Navbar.tsx
      to: frontend/src/pages/TransportPage.tsx
      via: "Link to='/transport'"
      pattern: "/transport"
    - from: frontend/src/App.tsx
      to: frontend/src/pages/TransportPage.tsx
      via: "<Route path='/transport'>"
      pattern: "Route path=\"/transport\""
---

<objective>
Build the Transportation Guide page — a static informational page covering how to get to and around European football stadiums. Add a link to it in the Navbar.

Purpose: PAGE-01 requires a transport guide accessible from navigation. This is a static page requiring no backend calls — pure React with Tailwind.

Output: /transport route renders a sectioned page; Navbar shows "Transport" link on every page.
</objective>

<execution_context>
@C:/Users/Yotam Shpilman/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Yotam Shpilman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-polish-and-launch/05-CONTEXT.md
@.planning/phases/05-polish-and-launch/05-RESEARCH.md

<interfaces>
<!-- Key contracts from existing code. No codebase exploration needed. -->

From frontend/src/components/Navbar.tsx (current nav links section):
```tsx
<div className="flex items-center gap-6">
  <Link to="/" className="text-gray-600 hover:text-green-600 transition-colors">Home</Link>
  <Link to="/fanbase" className="text-gray-600 hover:text-green-600 transition-colors">FanBase</Link>
  {/* auth section ... */}
</div>
```

Framer Motion page animation (all pages use this exact pattern):
```tsx
<motion.div
  initial={{ opacity: 0, y: 12 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.3 }}
  className="min-h-screen bg-gray-50"
>
```

User decision from CONTEXT.md (locked):
- 3 category headers: Public Transit, Ride Services, Long-Distance Travel
- Each service: card with app name + one-line description + deep link
- Content: generic European services (Uber, Bolt, Trainline, FlixBus, Omio, Citymapper)
- No city-specific content
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TransportPage component</name>
  <files>frontend/src/pages/TransportPage.tsx</files>
  <action>
    Create TransportPage.tsx. Use the same Framer Motion entry animation and page structure as other pages.

    Services to include (realistic European stadium-goer apps):

    PUBLIC TRANSIT section:
    - Citymapper — "The best public transit app for European cities. Covers metro, bus, tram, and bike. Works offline." — link: https://citymapper.com
    - Google Maps — "Works everywhere in Europe. Tap 'Directions' and choose the transit icon for step-by-step journey planning." — link: https://maps.google.com

    RIDE SERVICES section:
    - Uber — "Available in most European cities. Book a ride directly to the stadium — book early on match days as surge pricing applies." — link: https://uber.com
    - Bolt — "Often cheaper than Uber. Strong coverage in Central and Eastern Europe." — link: https://bolt.eu
    - Free Now (MyTaxi) — "Official licensed taxis across Western Europe. No surge pricing." — link: https://free-now.com

    LONG-DISTANCE TRAVEL section:
    - Trainline — "Book train tickets across Europe in one place. Early booking gets the cheapest fares." — link: https://trainline.com
    - FlixBus — "Affordable long-distance coaches connecting hundreds of European cities. Book online or in-app." — link: https://flixbus.com
    - Omio — "Compare trains, buses, and flights across Europe. Stores tickets in the app for easy access." — link: https://omio.com

    Page structure:
    - Page header: "Getting to the Match" + subtitle "Everything you need to get around European cities on match day"
    - max-w-4xl mx-auto px-4 py-8
    - Each section: heading with bottom border, then a grid of cards
    - Each service card: white bg, rounded-xl shadow-sm, p-6 — shows service name as h3, description as p text-gray-600, and a green "Open app" or "Visit website" link (target="_blank" rel="noopener noreferrer")
    - Cards in a responsive grid: sm:grid-cols-2 gap-4

    ```tsx
    import React from 'react';
    import { motion } from 'framer-motion';

    // Each service has a name, short description, and link to its website or app.
    interface TransportService {
      name: string;
      description: string;
      url: string;
      linkText: string;
    }

    // Service data for each of the three sections
    const PUBLIC_TRANSIT: TransportService[] = [ ... ];
    const RIDE_SERVICES: TransportService[] = [ ... ];
    const LONG_DISTANCE: TransportService[] = [ ... ];

    // Reusable card for a single service
    const ServiceCard: React.FC<{ service: TransportService }> = ({ service }) => (
      <div className="bg-white rounded-xl shadow-sm p-6">
        <h3 className="text-lg font-semibold text-gray-800 mb-2">{service.name}</h3>
        <p className="text-gray-600 text-sm mb-4">{service.description}</p>
        <a
          href={service.url}
          target="_blank"
          rel="noopener noreferrer"
          className="text-sm text-green-600 font-medium hover:text-green-700 transition-colors"
        >
          {service.linkText} →
        </a>
      </div>
    );

    // Section with a title and grid of service cards
    const TransportSection: React.FC<{ title: string; services: TransportService[] }> = ({ title, services }) => (
      <section className="mb-10">
        <h2 className="text-xl font-bold text-gray-800 mb-4 pb-2 border-b border-gray-200">{title}</h2>
        <div className="grid sm:grid-cols-2 gap-4">
          {services.map((service) => (
            <ServiceCard key={service.name} service={service} />
          ))}
        </div>
      </section>
    );

    export const TransportPage: React.FC = () => {
      return (
        <motion.div
          initial={{ opacity: 0, y: 12 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.3 }}
          className="min-h-screen bg-gray-50"
        >
          <div className="max-w-4xl mx-auto px-4 py-8">
            <div className="mb-8">
              <h1 className="text-3xl font-bold text-gray-800 mb-2">Getting to the Match</h1>
              <p className="text-gray-600">Everything you need to get around European cities on match day.</p>
            </div>
            <TransportSection title="Public Transit" services={PUBLIC_TRANSIT} />
            <TransportSection title="Ride Services" services={RIDE_SERVICES} />
            <TransportSection title="Long-Distance Travel" services={LONG_DISTANCE} />
          </div>
        </motion.div>
      );
    };
    ```

    Fill in the service arrays with the data listed above before exporting.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>TransportPage.tsx created with 3 sections (8 service cards total), TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Wire /transport route and add Navbar link</name>
  <files>
    frontend/src/App.tsx
    frontend/src/components/Navbar.tsx
  </files>
  <action>
    Step A — App.tsx:
    - Add import: `import { TransportPage } from './pages/TransportPage';`
    - Add route before the catch-all path="*": `<Route path="/transport" element={<TransportPage />} />`
    - Also add the /contact route (for Plan 02's ContactPage — import it here too so App.tsx is wired for all new pages):
      `<Route path="/contact" element={<ContactPage />} />`
      Import: `import { ContactPage } from './pages/ContactPage';`
      Note: ContactPage.tsx will be created in Plan 04. Add the route now so App.tsx is ready — the TypeScript error will be resolved when the file is created.
    - IMPORTANT: keep path="*" as the absolute last Route

    Actually — to avoid a TypeScript error from importing a file that doesn't exist yet, only add /transport and /register routes in this plan. ContactPage will be imported in Plan 04 when the file is created.

    So in this plan, App.tsx changes:
    - Add import for TransportPage
    - Add `<Route path="/transport" element={<TransportPage />} />` before the catch-all

    Step B — Navbar.tsx:
    - Add a "Transport" link between the FanBase link and the auth section:
      ```tsx
      <Link to="/transport" className="text-gray-600 hover:text-green-600 transition-colors">
        Transport
      </Link>
      ```
    - Keep all existing nav items unchanged
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>App.tsx has /transport route, Navbar shows Transport link. TypeScript compiles clean. Browser: /transport renders page with 3 sections, Navbar shows Transport on every page.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — TypeScript compiles clean
2. Manual: Navigate to /transport — page shows 3 section headers and service cards
3. Manual: Navbar visible on /transport page with Transport link highlighted
4. Manual: Click a service link — opens in new tab (target="_blank")
</verification>

<success_criteria>
- /transport route renders TransportPage with Public Transit, Ride Services, Long-Distance Travel sections
- Each section has at least 2 service cards with name, description, and external link
- Transport link visible in Navbar on every page
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-launch/05-03-SUMMARY.md`
</output>
